// The sample program: typed variables, integer arithmetic and printing.
fn main() {
  int x = -10;
  int y = 20;
  string s = "Hello World";
  println(x + y);
  println(s);
  bool isReady = true;
  println(isReady);

  int i = 3;
  while (i > 0) {
    println(i);
    i = i - 1;
  }
}
