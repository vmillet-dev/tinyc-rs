// Booleans: literals, variables, reassignment and printing.
fn main() {
  bool ready = true;
  bool done = false;
  println(ready);    // true
  println(done);     // false
  done = true;
  println(done);     // true
  println(false);    // a literal can be printed directly
}
